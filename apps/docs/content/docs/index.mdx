---
title: Welcome to Linkly
description: A URL shortener tutorial that teaches the iii engine from first function to full-stack deployment.
---

## What is Linkly?

Linkly is a URL shortener built step-by-step to teach the **iii** engine — a runtime that replaces your API framework, task queue, cron scheduler, pub/sub, state store, and observability pipeline with three primitives: **Function**, **Trigger**, **Worker**.

## Tutorial Chapters

Each chapter adds a new capability:

1. **Foundations** — Worker, functions, triggers, HTTP endpoints
2. **Observe everything** — Console, logs, traces, OpenTelemetry
3. **Persist everything** — SQLite database, cache-ahead reads
4. **Make it durable** — Queues, pub/sub, reactive state
5. **Stream live clicks** — Real-time streaming with iii-stream
6. **Move bulk data with channels** — CSV import over channels
7. **Bring in the browser** — Browser tab as an iii worker (Vite)
8. **Go full-stack with Next.js** — Turborepo, server actions, relay

## Architecture

By the end of the tutorial, you'll have:

- A Next.js frontend on Vercel
- An iii backend on Railway with multiple workers
- Shared type contracts between frontend and backend
- Real-time click streaming via a WebSocket relay
- Interactive API documentation with Scalar
