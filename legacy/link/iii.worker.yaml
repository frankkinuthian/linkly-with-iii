# Default standalone-worker manifest. Edit name, language, and entry as needed.
name: link
runtime:
  kind: typescript
  package_manager: npm
  entry: ./src/index.ts
scripts:
  install: "npm install"
  start: "npm run start"