# iii engine configuration
# Docs: https://iii.dev/docs/how-to/configure-engine
workers:
  - name: iii-observability
    config:
      enabled: true
      service_name: iii
      exporter: memory
      memory_max_spans: 10000
      metrics_enabled: true
      metrics_exporter: memory
      logs_enabled: true
      logs_exporter: memory
      logs_console_output: true
      sampling_ratio: 1.0

  - name: iii-queue
    config:
      queue_configs:
        clicks:
          type: standard
          max_retries: 5
          concurrency: 5
      adapter:
        name: builtin
  - name: iii-http
    config:
      port: 3111
      host: 127.0.0.1
      default_timeout: 30000
      concurrency_request_limit: 1024
      cors:
        allowed_origins:
          - "*"
        allowed_methods:
          - GET
          - POST
          - PUT
          - DELETE
          - OPTIONS
  - name: iii-state
    config:
      adapter:
        name: kv
        config:
          store_method: in_memory
          file_path: ./data/state_store.db
  - name: link
    worker_path: /Users/frank/Desktop/builds/three-eye-framework/linkly/link
  - name: database
    config:
      databases:
        primary:
          pool:
            acquire_timeout_ms: 5000
            idle_timeout_ms: 30000
            max: 10
          url: sqlite:./data/iii.db
        analytics:
          url: sqlite:./data/analytics.db
  - name: iii-pubsub
    config:
      adapter:
        name: local
  - name: analytics
    worker_path: /Users/frank/Desktop/builds/three-eye-framework/linkly/analytics
  - name: iii-stream
    config:
      port: 3112
      host: 127.0.0.1
      adapter:
        name: kv
        config:
          store_method: file_based
          file_path: ./data/stream_store
  - name: click-streamer
    worker_path: /Users/frank/Desktop/builds/three-eye-framework/linkly/click-streamer
